package academy.mindswap;

import java.lang.annotation.AnnotationTypeMismatchException;

public class Main {

    public static void main(String[] args) {
	// write your code here

        Card card = new Card();
        Atm atm = new Atm();

            atm.introduceCard(card);
            atm.introducePin(pin);
            atm.withdraw(100);


        client.withdraw(atm, 100);
            atm.introduceCard(card);
            atm.introducePin(pin);
            atm.withdraw(100);

        client.checkBalance(atm);
          if(validateATM(atm))
            atm.checkBalance(100);

           boolean validateATM(atm){
             atm.introduceCard(card);
             atm.introducePin(pin);
            }

        OU
        client.use(atm);
        client.introduceCard();
            this.atm.introduceCard(card);
        client.introducePin();
            this.atm.introducePin(pin);
        client.withdraw(100);
            this.atm.withdraw(100);
        client.removeCard();
            this.atm = null;


        ATM
                property: Card



    }
}
