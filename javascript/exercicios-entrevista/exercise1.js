/**
 * Task
 * 1. Declare two variables to represent the data above and assign them with the correct values.
 * 2. Print the two variables to the console.
 */

function getPassengerInformation() {
  let passengerName = "Tiago";
  let passengerAge = 34;

  return (`Passenger name: ${passengerName} Passsenger Age: ${passengerAge}`);
}

window.getPassengerInformation = getPassengerInformation;